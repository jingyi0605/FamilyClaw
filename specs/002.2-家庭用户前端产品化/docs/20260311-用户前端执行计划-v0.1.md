# 20260311-用户前端执行计划-v0.1

## 1. 这份文档是干什么的

这不是讨论稿，是给开发直接照着排活的。

目标只有一个：

把 `user-web` 现在还没收口的事情，拆成一批能直接开干、能单独验收、做完就能回写状态的任务。

---

## 2. 执行原则

这轮按下面的顺序推进，不要并行乱铺：

1. 先补主链路，优先把“看状态 + 改配置”做完整
2. 再补助手和记忆，把它从可演示补到可持续使用
3. 最后做体验统一和文档验收收口

控制范围：

- 不为了前端补齐去临时扩后端大接口
- 没有稳定后端承载的字段，不做假保存
- 每做完一步，都要能单独演示和单独验证

---

## 3. 第一批：先把阶段 2 真正收口

这批做完，`2.4` 才有资格进入检查。

### 3.1 任务 A：补家庭基础信息编辑

- 这一步做什么：在家庭概览里补家庭基础信息编辑入口，至少支持修改家庭名称、语言、时区这类已经有稳定承载的字段。
- 做完后能看到什么：家庭页不再只有只读卡片，用户能在 `FamilyOverview` 里保存家庭基础信息。
- 依赖什么：现有 `households` 读接口已经接通；如果没有对应写接口，要先确认是否复用现有接口，否则这一步只做 UI 方案，不做假提交。
- 当前阻塞：已检查 `apps/api-server/app/api/v1/endpoints/households.py`，目前只有 `GET /households`、`GET /households/{id}` 和 `POST /households`，没有家庭更新接口。这一步暂时受后端接口阻塞，先不做假保存。
- 主要改哪里：
  - `apps/user-web/src/pages/FamilyPage.tsx`
  - `apps/user-web/src/lib/api.ts`
- 明确先不做什么：
  - 先不做家庭删除
  - 先不做复杂权限分级
- 怎么验证：
  - 构建通过：`cd apps/user-web && npm.cmd run build`
  - 人工检查：进入家庭概览，修改后能保存、能回显、失败时有提示

### 3.2 任务 B：补房间编辑，不再只有新增

- 这一步做什么：把房间卡片上的禁用“编辑”按钮改成可用，至少支持编辑房间名、房间类型、隐私等级。
- 做完后能看到什么：房间页既能新增，也能维护已有房间。
- 依赖什么：任务 A 不强依赖，但建议同批推进；如果没有现成房间更新接口，要先确认后端是否已有可复用能力。
- 当前阻塞：已检查 `apps/api-server/app/api/v1/endpoints/rooms.py`，目前只有 `GET /rooms` 和 `POST /rooms`，没有房间更新接口。这一步暂时受后端接口阻塞，先不做假保存。
- 主要改哪里：
  - `apps/user-web/src/pages/FamilyPage.tsx`
  - `apps/user-web/src/lib/api.ts`
- 明确先不做什么：
  - 先不做房间排序拖拽
  - 先不做房间删除
- 怎么验证：
  - 构建通过
  - 人工检查：编辑已有房间后列表刷新正确，校验和错误提示正常

### 3.3 任务 C：补首页快捷动作跳转

- 这一步做什么：把首页快捷操作区接到真实页面跳转。
- 做完后能看到什么：点“助手 / 记忆 / 设置 / 家庭”会直接进入对应页面。
- 依赖什么：无强依赖。
- 主要改哪里：
  - `apps/user-web/src/pages/HomePage.tsx`
- 明确先不做什么：
  - 先不在首页直接触发复杂业务弹层
- 怎么验证：
  - 构建通过
  - 人工检查 4 个按钮都能正确跳转

### 3.4 任务 D：清理设置页里的假表单，能接真就接真，不能接真就别装能保存

- 这一步做什么：把设置页拆成两类字段处理：
  1. 后端已有稳定承载的，接成真保存
  2. 后端还没有稳定承载的，从“可编辑表单”改成“说明态”或明确标记暂未开放
- 做完后能看到什么：设置页不会再出现一堆看起来能改、其实没保存的字段。
- 依赖什么：现有 `context/configs` 已经接通；如 `ai/runtime-defaults`、`ai/routes` 确认可用，就一并补接。
- 主要改哪里：
  - `apps/user-web/src/pages/SettingsPage.tsx`
  - `apps/user-web/src/lib/api.ts`
- 建议拆成 3 个小步：
  1. 先梳理 AI 页字段，分清哪些接真、哪些降级展示
  2. 再梳理通知页字段，去掉假保存入口
  3. 最后补接口说明文案和风险提示
- 明确先不做什么：
  - 先不搬 provider 调试配置
  - 先不做“假本地保存”冒充真实配置
- 怎么验证：
  - 构建通过
  - 人工检查：每一个可编辑项都能说明是否真实保存；不能保存的项不会误导用户

### 3.5 任务 E：补手机端家庭切换入口

- 这一步做什么：给移动端补一个能切当前家庭的入口，别再依赖桌面侧边栏。
- 做完后能看到什么：手机上也能切家庭，首页、家庭页、设置页会跟着刷新。
- 依赖什么：无。
- 主要改哪里：
  - `apps/user-web/src/components/ShellNav.tsx`
  - `apps/user-web/src/layouts/AppLayout.tsx`
  - `apps/user-web/src/styles.css`
- 明确先不做什么：
  - 先不做复杂的家庭管理抽屉
- 怎么验证：
  - 构建通过
  - 人工检查手机断点下可以切家庭，且切换后各页数据同步变化

### 3.6 第一批完成线

第一批做完，至少要满足下面 4 条：

1. 首页快捷动作全部能进真实页面
2. 家庭页能编辑家庭主体和已有房间
3. 设置页里不再有误导性的假保存项
4. 手机端可以切家庭

满足这 4 条后，再回写 `tasks.md` 里的 `2.1`、`2.2`、`2.3`、`2.4`。

---

## 4. 第二批：把阶段 3 补到可持续使用

这批做完，`3.4` 才有资格检查。

### 4.1 任务 F：给助手补会话持久化

- 这一步做什么：先补最小可用的会话持久化，至少做到刷新后还能看到最近会话和消息。
- 做完后能看到什么：助手不再是一次性聊天框。
- 依赖什么：无；如果后端没有会话接口，这轮先做本地持久化也可以，但文档里必须写清是临时方案。
- 主要改哪里：
  - `apps/user-web/src/pages/AssistantPage.tsx`
  - 如有必要，可新增 `apps/user-web/src/state/` 或 `apps/user-web/src/lib/` 下的会话存储文件
- 建议最小实现：
  1. 以 `household_id` 为维度保存最近会话
  2. 保存会话标题、时间、消息列表
  3. 页面加载时自动恢复最近一次活跃会话
- 明确先不做什么：
  - 先不追求服务端多端同步
- 怎么验证：
  - 构建通过
  - 人工检查：提问后刷新页面，会话和消息仍在

### 4.2 任务 G：补助手页跳转相关页面动作

- 这一步做什么：在助手回答后的动作区补“去家庭页 / 去设置页 / 去记忆页”这类明确跳转。
- 做完后能看到什么：助手回答后，用户能继续去相关页面处理问题。
- 依赖什么：任务 F 不强依赖，但建议一起做。
- 主要改哪里：
  - `apps/user-web/src/pages/AssistantPage.tsx`
  - 如有必要，可新增 `apps/user-web/src/lib/actions/`
- 明确先不做什么：
  - 先不做高风险自动执行
- 怎么验证：
  - 构建通过
  - 人工检查：从回答能跳到对应页面，跳转路径清楚

### 4.3 任务 H：评估并补场景动作入口

- 这一步做什么：先别急着写复杂“场景执行”，先确认当前仓库里有没有稳定的场景产品语义和接口；有就接最小入口，没有就把它明确写成后续缺口。
- 做完后能看到什么：团队知道“场景动作”这项是本轮可做，还是明确延期，而不是一直挂着一句空话。
- 依赖什么：需要先确认后端接口和产品边界。
- 主要改哪里：
  - `apps/user-web/src/pages/AssistantPage.tsx`
  - `apps/user-web/src/lib/api.ts`
  - `specs/002.2-家庭用户前端产品化/tasks.md`
- 明确先不做什么：
  - 没有稳定语义时，不硬做一个假场景按钮
- 怎么验证：
  - 如果本轮可做：能演示一个最小场景动作入口
  - 如果本轮不做：`tasks.md` 和文档里明确记为缺口，不再含糊

### 4.4 任务 I：补记忆页权限提示和编辑说明

- 这一步做什么：把记忆页里“哪些能改、哪些不能改、为什么”说清楚，避免继续像管理台。
- 做完后能看到什么：用户在记忆详情里能清楚理解当前权限边界。
- 依赖什么：无。
- 主要改哪里：
  - `apps/user-web/src/pages/MemoriesPage.tsx`
- 建议最小实现：
  1. 在详情区增加当前操作权限说明
  2. 对不可操作按钮给出明确原因
  3. 对删除能力增加用户态提示，不只靠 actor role 硬开关
- 明确先不做什么：
  - 先不做复杂协作审批流
- 怎么验证：
  - 构建通过
  - 人工检查：不同状态记忆的提示文案清楚，操作结果反馈明确

### 4.5 第二批完成线

第二批做完，至少要满足下面 4 条：

1. 助手刷新后还能保留最近会话
2. 助手回答后至少能跳转相关页面
3. 场景动作是否纳入本轮已经明确，不再悬空
4. 记忆页权限提示和编辑说明明显更清楚

满足这 4 条后，再回写 `3.1`、`3.2`、`3.3`、`3.4`。

---

## 5. 第三批：做产品化收口

这批做完，阶段 4 才算真的启动。

### 5.1 任务 J：统一空态、错误态、骨架屏和降级提示

- 这一步做什么：抽一套统一的反馈组件和文案规则，然后把首页、家庭、助手、记忆、设置逐页替换。
- 做完后能看到什么：不同页面失败时的表现不再各说各话。
- 依赖什么：建议在前两批完成后做，避免重复改。
- 主要改哪里：
  - `apps/user-web/src/components/feedback/`（建议新增）
  - `apps/user-web/src/pages/HomePage.tsx`
  - `apps/user-web/src/pages/FamilyPage.tsx`
  - `apps/user-web/src/pages/AssistantPage.tsx`
  - `apps/user-web/src/pages/MemoriesPage.tsx`
  - `apps/user-web/src/pages/SettingsPage.tsx`
- 明确先不做什么：
  - 先不追求复杂动画
- 怎么验证：
  - 构建通过
  - 人工制造接口失败，检查提示风格一致

### 5.2 任务 K：补无障碍和关键交互键盘可达性

- 这一步做什么：把关键按钮、弹层、导航、切换器的语义和键盘操作补齐。
- 做完后能看到什么：核心操作不只鼠标能点，键盘也能走通。
- 依赖什么：建议在任务 J 后做，便于统一处理组件语义。
- 主要改哪里：
  - `apps/user-web/src/components/base.tsx`
  - `apps/user-web/src/pages/`
  - `apps/user-web/src/styles.css`
- 明确先不做什么：
  - 先不追求完整无障碍审计报告
- 怎么验证：
  - 构建通过
  - 人工键盘走查：首页导航、弹层开关、设置切换器、助手输入都能操作

### 5.3 第三批完成线

第三批做完，至少要满足下面 3 条：

1. 主页面的空态、错误态、降级提示一致
2. 关键页面的主要操作能用键盘完成
3. `4.1` 和 `4.2` 可以开始回写真实状态

---

## 6. 第四批：文档和验收收口

这批不改主页面功能，但必须做，不然后面接手会很痛苦。

### 6.1 任务 L：回写 `tasks.md`

- 这一步做什么：把前 3 批实际完成情况回写到 `tasks.md`，别再让任务状态落后于代码。
- 主要改哪里：
  - `specs/002.2-家庭用户前端产品化/tasks.md`
- 怎么验证：
  - 人工走查任务状态、当前进度、卡点是否和代码一致

### 6.2 任务 M：补联调说明

- 这一步做什么：写一份接手人一看就能跑的联调说明。
- 至少包含：
  - `user-web` 启动方式
  - 依赖的环境变量
  - 依赖的后端接口
  - 当前还没完成的配置项和降级项
- 主要改哪里：
  - `specs/002.2-家庭用户前端产品化/docs/`
- 怎么验证：
  - 没参与开发的人按文档能独立跑起来

### 6.3 任务 N：补视觉走查和验收记录

- 这一步做什么：把桌面、平板、手机、长辈模式和五大入口的验收结论写出来。
- 主要改哪里：
  - `specs/002.2-家庭用户前端产品化/docs/`
- 怎么验证：
  - 文档里能清楚看到“测了什么、结果怎样、还剩什么限制”

### 6.4 第四批完成线

第四批做完，至少要满足下面 3 条：

1. `tasks.md` 状态和代码一致
2. 联调说明能指导别人接手
3. 有明确的视觉走查和验收记录

---

## 7. 推荐排期方式

如果按最省返工的方式排，建议这样走：

### 第 1 周

- 任务 A：补家庭基础信息编辑
- 任务 B：补房间编辑
- 任务 C：补首页快捷动作跳转

### 第 2 周

- 任务 D：清理设置页假表单
- 任务 E：补手机端家庭切换入口

### 第 3 周

- 任务 F：补助手会话持久化
- 任务 G：补助手页跳转动作
- 任务 H：明确场景动作本轮边界

### 第 4 周

- 任务 I：补记忆页权限提示和编辑说明
- 任务 J：统一反馈组件
- 任务 K：无障碍收口

### 第 5 周

- 任务 L：回写 `tasks.md`
- 任务 M：补联调说明
- 任务 N：补视觉走查和验收记录

如果人手不够，就优先保住前两周内容，因为那部分最直接决定阶段 2 能不能收口。

---

## 8. 开发时的注意事项

- 每做完一个任务，就马上回写状态，不要等最后一起补
- 新增接口调用前，先确认后端已有稳定语义，再决定是“接真”还是“明确降级”
- 不要把用户端又做回管理台，能说人话就别露底层术语
- 移动端适配时，优先保关键操作可达，不要先卷动画

---

## 9. 一句话结论

接下来最正确的做法，不是继续散着补页面，而是按“阶段 2 收口 -> 阶段 3 收口 -> 阶段 4 收口”的顺序，把每一步都拆成能独立提交、独立验证、独立回写状态的小任务。
