# 20260319-验收清单

这份清单分两部分：

- 已经做完的自动化验收
- 还需要在真机或可运行环境里做的 H5 / RN 手工验收

## 自动化验收

- [x] 成员级导览状态字段通过 Alembic migration 落库
- [x] `packages/user-core` 已暴露 `guideStatus`，前端启动快照可直接消费
- [x] 共享导览脚本、共享状态机、H5/RN 平台适配器结构已经拆开
- [x] `npm.cmd --prefix ./apps/user-app run test:guide-runtime` 通过
- [x] `npm.cmd --prefix ./apps/user-app run typecheck` 通过
- [x] `C:\Code\FamilyClaw\apps\api-server\.venv\Scripts\python.exe -m unittest tests.test_member_guide_status_api` 通过
- [x] 正式文档已补：
  - `docs/Documentation/开发指南/前端开发.md`
  - `docs/Documentation/使用指南/家庭与成员管理.md`
  - `docs/Documentation/参考资料/接口参考.md`
- [x] Spec 补充文档已补：
  - `specs/001.9-初始化完成后跨端新手向导/docs/20260319-联调说明.md`
  - `specs/001.9-初始化完成后跨端新手向导/docs/20260319-验收清单.md`

## H5 手工验收

- [ ] 新成员完成初始化后首次进入应用，首页自动拉起导览
- [ ] 导览能按顺序经过：首页 -> 家庭 -> 助手 -> 记忆 -> 设置
- [ ] 刷新页面后能恢复到最近一步
- [ ] 单个 anchor 缺失时会自动退回页面级说明
- [ ] 跳过后会写回成员导览版本，并清理本地标记
- [ ] 设置页“重新查看导览”能重新拉起
- [ ] 已完成当前版本导览的历史成员再次进首页时不会被重复打断

## RN 手工验收

- [ ] 未完成当前导览版本的成员进入应用后能拉起导览
- [ ] 同一套共享脚本能带着页面链路走到设置页
- [ ] 占位页 / 页面级 anchor 场景不会中断链路
- [ ] 切页后能继续当前步骤
- [ ] 设置页手动重开入口可用
- [ ] 单个 anchor 缺失时会回退到页面级说明

## 验收结论口径

当前代码、接口、文档和自动化测试已经收口完成。

剩下未勾选的项目不是“实现没做”，而是“还没在真实运行环境里手工回放”。只要下一步进入 H5 / RN 联调环境，按这份清单走完，就可以把阶段 2.3 和最终检查一起正式关掉。
