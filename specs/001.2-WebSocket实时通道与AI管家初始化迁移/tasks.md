# 任务文档 - WebSocket 实时通道与 AI 管家初始化迁移

状态：Draft

## 当前判断

这份 Spec 不是“把 SSE 改成 WebSocket”这么简单。  
真正要做的是把实时协议、会话持久化、轮次状态、页面恢复一次理顺，不然只是换个壳继续烂。

---

## 阶段 1：先把实时协议和会话模型定下来

- [ ] 1.1 定义统一实时事件协议
  - 状态：TODO
  - 这一步到底做什么：把实时事件的公共结构、事件类型、字段约束先定死，并明确展示文本和结构化状态必须走两条分开的事件链路。
  - 做完你能看到什么：不再出现每个接口自己拼 `chunk`、`done`、`error` 的临时 JSON，也不会再把 `<config>` 这种控制协议塞进展示文本。
  - 先依赖什么：无
  - 开始前先看：
    - `requirements.md` 需求 1、需求 2
    - `design.md` §2.1、§3.3.4
  - 主要改哪里：
    - `apps/api-server/app/modules/realtime/`
    - `apps/user-web/src/lib/`
  - 这一步先不做什么：先不接 AI 供应商，也不处理页面细节。
  - 怎么算完成：
    1. 事件结构、事件类型和基础校验已经落地
    2. 前后端共享同一套事件语义
    3. `agent.chunk` 与 `agent.state_patch` 的职责边界已经写死
  - 怎么验证：
    - 后端单元测试
    - 前端类型检查
  - 对应需求：`requirements.md` 需求 1、需求 2、需求 2.1
  - 对应设计：`design.md` §2.1、§2.1.1、§3.3.4

- [ ] 1.2 建立初始化会话、消息、轮次的数据模型
  - 状态：TODO
  - 这一步到底做什么：把“会话”“消息”“请求轮次”从一坨 JSON 里拆出来，让恢复和排查有明确落点。
  - 做完你能看到什么：数据库里能清楚看出这轮是谁发的、回了什么、有没有结束。
  - 先依赖什么：1.1
  - 开始前先看：
    - `requirements.md` 需求 3、需求 4
    - `design.md` §3.2、§4.1、§4.2
    - `apps/api-server/migrations/20260311-数据库迁移规范.md`
  - 主要改哪里：
    - `apps/api-server/app/modules/agent/` 或新的 `agent_bootstrap/`
    - `apps/api-server/migrations/versions/`
  - 这一步先不做什么：先不迁移前端页面。
  - 怎么算完成：
    1. 数据库里有独立的消息表和请求轮次表
    2. 现有会话表能记录当前轮次和事件序号
  - 怎么验证：
    - `python -m alembic upgrade head`
    - 人工检查表结构和基础读写
  - 对应需求：`requirements.md` 需求 3、需求 4
  - 对应设计：`design.md` §3.2、§4.1、§4.2

### 阶段检查

- [ ] 1.3 阶段检查：确认实时基础层已经站稳
  - 状态：TODO
  - 这一步到底做什么：检查现在是不是已经有一套明确的协议和数据模型，而不是边写边猜。
  - 做完你能看到什么：后面迁移 AI 对话时不会再把结构问题带进去。
  - 先依赖什么：1.1、1.2
  - 开始前先看：
    - `requirements.md`
    - `design.md`
    - `tasks.md`
  - 主要改哪里：本阶段相关文件
  - 这一步先不做什么：不开始接页面。
  - 怎么算完成：
    1. 事件协议和数据模型已经能支撑一轮完整对话
    2. 已知边界和风险已经写清楚
  - 怎么验证：
    - 人工走查
  - 对应需求：`requirements.md` 需求 1、需求 3、需求 4
  - 对应设计：`design.md` §2、§3.2、§4

---

## 阶段 2：把后端实时链路迁到 WebSocket

- [ ] 2.1 建 WebSocket 入口和连接管理
  - 状态：TODO
  - 这一步到底做什么：把鉴权、会话订阅、事件发送、连接关闭这些公共问题收口到统一管理里。
  - 做完你能看到什么：后端已经能建立稳定的初始化实时连接，不再依赖 SSE 端点。
  - 先依赖什么：1.3
  - 开始前先看：
    - `requirements.md` 需求 1、需求 4
    - `design.md` §2.2、§3.1、§3.3.1
  - 主要改哪里：
    - `apps/api-server/app/modules/realtime/`
    - `apps/api-server/app/api/`
  - 这一步先不做什么：先不把所有业务都迁上来。
  - 怎么算完成：
    1. 能建立带身份校验的 WebSocket 连接
    2. 能按 `session_id` 收发统一事件
  - 怎么验证：
    - WebSocket 集成测试
    - 人工握手和连接关闭测试
  - 对应需求：`requirements.md` 需求 1、需求 4
  - 对应设计：`design.md` §2.2、§3.1、§3.3.1

- [ ] 2.2 重写 AI 管家初始化实时执行器
  - 状态：TODO
  - 这一步到底做什么：把当前初始化对话的“收用户消息、跑 LLM、发 chunk、更新草稿、结束一轮”改成真正按 request_id 驱动，并彻底去掉标签协议。
  - 做完你能看到什么：一轮对话有明确 accepted、chunk、state_patch、done/error，不会再挂着不结束，也不会再在文本里看到 `<config>`、`<json>` 或 `---` 分隔块。
  - 先依赖什么：2.1
  - 开始前先看：
    - `requirements.md` 需求 2、需求 3
    - `design.md` §2.3.2、§4.2、§5.3、§6.1
  - 主要改哪里：
    - `apps/api-server/app/modules/agent/` 或新的 `agent_bootstrap/`
    - `apps/api-server/app/modules/llm_task/`
    - `apps/api-server/app/modules/ai_gateway/`
  - 这一步先不做什么：先不迁移别的 AI 场景。
  - 怎么算完成：
    1. 连续多轮初始化对话都能明确结束
    2. 消息和轮次状态能落库
    3. 文本落库不再乱码
    4. 展示文本与结构化状态已经彻底分离
    5. 供应商支持结构化输出时直接使用，不支持时走二次提取调用
  - 怎么验证：
    - 后端集成测试
    - 人工连续发送 20 轮消息检查无挂起
    - 人工检查展示文本中不再出现标签协议
  - 对应需求：`requirements.md` 需求 2、需求 2.1、需求 3
  - 对应设计：`design.md` §2.1.1、§2.3.2、§3.3.5、§4.2、§5.3、§6.1、§6.3、§6.4

### 阶段检查

- [ ] 2.3 阶段检查：确认后端 WebSocket 主链路已经可用
  - 状态：TODO
  - 这一步到底做什么：检查后端是否已经真正能稳定跑通一轮到多轮对话，而不是只建了个连接壳子。
  - 做完你能看到什么：前端接入前，后端主链路已经是可验证状态。
  - 先依赖什么：2.1、2.2
  - 开始前先看：
    - `requirements.md`
    - `design.md`
    - `tasks.md`
  - 主要改哪里：本阶段相关文件
  - 这一步先不做什么：不加新需求。
  - 怎么算完成：
    1. 一轮、多轮、失败轮次都能正确结束
    2. 快照数据和消息记录一致
  - 怎么验证：
    - 人工走查
    - 自动化测试回放
  - 对应需求：`requirements.md` 需求 2、需求 3、需求 4
  - 对应设计：`design.md` §2.3、§4、§5、§6

---

## 阶段 3：迁移前端初始化页面并补恢复能力

- [ ] 3.1 把 AI 管家初始化页面改成 WebSocket 客户端
  - 状态：TODO
  - 这一步到底做什么：让页面通过统一事件协议收消息、更新 UI、处理轮次结束，而不是继续读 SSE 流，也不再从文本里反解析状态。
  - 做完你能看到什么：初始化对话页面能稳定逐字输出，并能继续多轮对话，前端代码里不再有 `<config>` 清洗或文本反解析逻辑。
  - 先依赖什么：2.3
  - 开始前先看：
    - `requirements.md` 需求 1、需求 2
    - `design.md` §2.3.1、§3.3.1、§3.3.4
  - 主要改哪里：
    - `apps/user-web/src/components/ButlerBootstrapConversation.tsx`
    - `apps/user-web/src/lib/api.ts`
    - `apps/user-web/src/lib/` 下新的 realtime client
  - 这一步先不做什么：先不改其他实时页面。
  - 怎么算完成：
    1. 页面已经不再依赖 SSE
    2. 多轮对话过程不会卡死
    3. 页面只消费 `agent.chunk` 和 `agent.state_patch`，不再解析标签协议
  - 怎么验证：
    - `cd apps/user-web && npm.cmd run build`
    - 人工连续多轮测试
    - 人工检查前端不再依赖标签清洗逻辑
  - 对应需求：`requirements.md` 需求 1、需求 2、需求 2.1
  - 对应设计：`design.md` §2.1.1、§2.3.1、§3.3.1、§3.3.4、§3.3.5

- [ ] 3.2 做刷新恢复、断线恢复和重新开始
  - 状态：TODO
  - 这一步到底做什么：让页面刷新、重连、重新开始都以服务端快照为准，不再靠本地拼凑历史记录。
  - 做完你能看到什么：页面刷新后还能看到当前会话历史，重新开始会创建全新会话。
  - 先依赖什么：3.1
  - 开始前先看：
    - `requirements.md` 需求 3、需求 4、需求 5
    - `design.md` §2.3.3、§3.3.2、§3.3.3、§6.2
  - 主要改哪里：
    - `apps/user-web/src/components/ButlerBootstrapConversation.tsx`
    - `apps/api-server/app/api/v1/endpoints/ai_config.py`
    - `apps/api-server/app/modules/agent/` 或新的 `agent_bootstrap/`
  - 这一步先不做什么：先不扩到别的 Agent 对话。
  - 怎么算完成：
    1. 刷新页面后可恢复当前会话历史消息
    2. 断线重连后能恢复快照并继续使用
    3. 点击重新开始后旧会话不再继续追加消息
  - 怎么验证：
    - 人工刷新页面
    - 人工断网重连
    - 人工重新开始验证
  - 对应需求：`requirements.md` 需求 3、需求 4、需求 5
  - 对应设计：`design.md` §2.3.3、§3.3.2、§3.3.3、§6.2

### 最终检查

- [ ] 3.3 最终检查：确认初始化链路已经彻底脱离 SSE
  - 状态：TODO
  - 这一步到底做什么：确认 AI 管家初始化这条链路真的已经站稳，能作为后续实时能力的模板，而不是只是把问题换了个地方躲起来，同时确认标签协议已经从这条链路里被彻底清掉。
  - 做完你能看到什么：有一条稳定、可恢复、可复用的实时主链路。
  - 先依赖什么：3.1、3.2
  - 开始前先看：
    - `requirements.md`
    - `design.md`
    - `tasks.md`
    - `docs/`
  - 主要改哪里：当前 Spec 涉及的全部相关文件
  - 这一步先不做什么：不再顺手扩新功能。
  - 怎么算完成：
    1. AI 管家初始化对话已全面使用 WebSocket
    2. 历史消息恢复、重连恢复、重新开始都已跑通
    3. 已知风险和后续复用边界已记录清楚
    4. 代码、协议、数据库和前端展示链路里都不再依赖标签协议
  - 怎么验证：
    - 连续 20 轮人工回放
    - 前后端测试与联调记录
    - 全链路检查不再出现 `<config>`、`<json>`、`---` 分隔块
  - 对应需求：`requirements.md` 全部需求
  - 对应设计：`design.md` 全文
