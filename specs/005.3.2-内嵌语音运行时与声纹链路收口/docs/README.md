# 005.3.2 补充文档

这里放 `005.3.2` 后续联调、验收和迁移记录。

建议后续优先补这几类文档：

- `20260317-迁移验收清单.md`
  - 记录本地默认只启动 `api-server` 的验证步骤
  - 记录 `embedded / disabled` 两种模式的验收口径

- `20260317-阻塞边界回归记录.md`
  - 记录哪些音频落盘和声纹调用已经下沉到 blocking helper
  - 记录慢任务不拖死 HTTP / WebSocket 的回归结果

- `20260317-005.3回写清单.md`
  - 记录迁移完成后，`005.3` 哪些段落需要同步更新
  - 避免最后只改代码，不改旧 spec
